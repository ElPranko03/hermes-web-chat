Inhalt:

services:
  - type: web
    name: hermes
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free

  - type: web
    name: webui
    runtime: image
    image:
      url: ghcr.io/open-webui/open-webui:main
    plan: free
